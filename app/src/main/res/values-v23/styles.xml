<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppCusTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <!-- header date, month color && calendar text highlight color -->
        <item name="android:textColorPrimaryInverse">@color/colorPrimary</item>
    </style>

    <style name="LoginTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="WelcomeCustom" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@color/white_transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="WhiteTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:statusBarColor">@color/bg</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <style name="TextLabel" parent="Widget.MaterialComponents.TextInputLayout.FilledBox">
        <item name="android:textSize">@dimen/text_small</item>
        <item name="android:textColorHint">@color/secondaryText</item>
        <item name="colorControlNormal">@color/secondaryText</item>
        <item name="colorControlActivated">@color/secondaryText</item>
        <item name="android:colorControlHighlight">@color/secondaryText</item>
    </style>

    <!-- Theme.AppCompat.Light is not a dialog theme -->
    <style name="DatePickerTheme" parent="android:Theme.Material.Light.Dialog">
        <!-- this is new -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:datePickerStyle">@style/MyDatePickerStyle</item>
        <item name="android:colorAccent">@color/colorPrimaryDark</item>
    </style>

    <style name="MyDatePickerStyle" parent="@android:style/Widget.Material.Light.DatePicker">
        <!-- this is new -->
        <item name="android:headerBackground">@color/colorPrimary</item>
        <item name="android:calendarTextColor">@color/colorAccent</item>
        <item name="android:dayOfWeekBackground">@color/colorAccent</item>
        <item name="android:yearListSelectorColor">@color/colorPrimary</item>
        <item name="android:datePickerMode">calendar</item>
    </style>

</resources>